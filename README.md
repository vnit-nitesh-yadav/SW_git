# SW_git

A practice repository for learning and demonstrating **Git version control workflows** through sample code and files.  
This repo contains basic C code and text files to illustrate Git commands, branching, commits, and merges during practice sessions.

---

## 📌 Repository Purpose

This project was created to:
- Practice **Git basics:** clone, commit, branch, merge, push, pull
- Learn how to manage files and code using Git and GitHub
- Demonstrate simple version history changes during Git exercises

---

## 📁 Repository Contents

SW_git/
├── file1.txt.txt # Sample text file for Git operations
├── file2.txt # Another sample text file
├── mark-sweep-graph.c # C code demonstrating a mark & sweep graph (example)
└── README.md # Project documentation (this file)

yaml
Copy code

---

## 🛠️ What’s Inside

### 📄 Sample Text Files
`file1.txt.txt` and `file2.txt` are used to practice:
- Editing text files
- Committing changes
- Resolving merge conflicts  
(*Open locally to explore contents*)

### 🧠 C Code Example
`mark-sweep-graph.c` is a **C source file** included as an example for Git tracking and versioning of code in C.  
It may contain a sample implementation of a graph-related algorithm or mark-sweep technique for educational purposes.

---

## 🧪 How to Use

1. **Clone the repository:**
   ```bash
   git clone https://github.com/vnit-nitesh-yadav/SW_git.git
Explore files and make changes

Open text files or C code in your editor.

Practice Git commands locally.

Commit & push changes:

bash
Copy code
git add .
git commit -m "Your commit message"
git push
💡 Learn Git with This Repo
You can use this repository to practice:

git init, git add, git commit

Branching (git branch, git checkout)

Merging and conflict resolution

Rebase and history logs

📌 Tips
Create a new branch for every experiment

Track changes with git status and git log

Try collaborative workflows with pull requests

🚀 Next Steps
You can extend this repo by adding:

Additional code samples

Scripts for automation

Branching and tagging workflows
